#include <stdio.h>
#include <limits.h>

int main() {
    printf("Storage size for integer : %d \n", sizeof(int));
    printf("Minimum integer positive value : %d\n", INT_MIN);
    printf("Maximum integer positive value : %d\n", INT_MAX);

    return 0;
}